;; if you split the window on a dired buffer, any files you try copy or rename etc will
;; automatically have their target directory set as what the right dired buffer is on
(setq dired-dwim-target t)
